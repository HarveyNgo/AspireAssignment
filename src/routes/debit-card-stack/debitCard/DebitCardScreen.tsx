import {Container} from '@components';
import {Text} from 'react-native';

const DebitCardScreen = () => {
  return (
    <Container>
      <Text>this is debitCard screen</Text>
    </Container>
  );
};

export default DebitCardScreen;
